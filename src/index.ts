import { ColorHelper } from "./colorHelper";
import * as colorUtils from "./colorUtils";

import calculateHighlightColor = colorUtils.calculateHighlightColor;
import channelBlend = colorUtils.channelBlend;
import createLinearColorScale = colorUtils.createLinearColorScale;
import darken = colorUtils.darken;
import hexBlend = colorUtils.hexBlend;
import hexString = colorUtils.hexString;
import hexToRGBString = colorUtils.hexToRGBString;
import LinearColorScale = colorUtils.LinearColorScale;
import normalizeToHexString = colorUtils.normalizeToHexString;
import parseColorString = colorUtils.parseColorString;
import rgbBlend = colorUtils.rgbBlend;
import RgbColor = colorUtils.RgbColor;
import rgbString = colorUtils.rgbString;
import rotate = colorUtils.rotate;
import shadeColor = colorUtils.shadeColor;

export {
    ColorHelper,
    calculateHighlightColor,
    channelBlend,
    createLinearColorScale,
    darken,
    hexBlend,
    hexString,
    hexToRGBString,
    LinearColorScale,
    normalizeToHexString,
    parseColorString,
    rgbBlend,
    RgbColor,
    rgbString,
    rotate,
    shadeColor
};